# Nebius inference

`client.ts` only validates configuration on demand; it does not call a model. Add the current supported SDK/API, timeouts, bounded retries, runtime schema validation, and usage tracking. Choose an available NVIDIA open-source model in the Nebius console and record the exact model ID for the demo. Never expose credentials with NEXT_PUBLIC_ variables or log configuration objects.
